## 0.8.11 Beta

- Фирменный экран LinkVideo теперь включается только по системному событию отключения дисплея Windows.
- Обычная блокировка Win+L снова показывает настоящий экран блокировки Windows.
- После включения мониторов захват Winlogon восстанавливается без перезапуска RTSP/RTMP-потока.
- Для Windows 8+ используется `GUID_CONSOLE_DISPLAY_STATE`, для Windows 7 — совместимый `GUID_MONITOR_POWER_ON`.
- Удалено ошибочное определение выключенного экрана только по чёрному кадру Desktop Duplication.
- Добавлен новый полноэкранный фирменный фон LinkVideo для состояния выключенных дисплеев.

## 0.8.10 Beta

- при обычной блокировке Windows по-прежнему показывается настоящий экран Winlogon;
- если после блокировки физические мониторы погасли и Windows перестала отдавать изображение, вместо чёрного кадра автоматически показывается фирменная плашка LinkVideo;
- текст плашки: «Экран заблокирован» и «Трансляция восстановится при входе в систему»;
- чёрные кадры защищённого рабочего стола больше не принимаются как корректное изображение;
- при захвате нескольких мониторов логотип и сообщение выводятся отдельно на каждом экране, а не на стыке виртуального рабочего стола;
- переключение на плашку и обратно не перезапускает FFmpeg и не разрывает RTSP/RTMP-соединение.

## 0.8.9 Beta

- подпись локальной трансляции сокращена до «Ссылка для LinkVideo Server»;
- убраны постоянные служебные подсказки о количестве кодировщиков, смешивании аудио и свободном RTSP-порте; занятый порт по-прежнему показывается как ошибка;
- возвращена инструкция по перемещению индикатора записи мышью;
- микрофон может передаваться самостоятельно, даже когда системный звук отключён;
- запуск и перезапуск потока доступны клиенту без пароля администратора; остановка потока остаётся защищённой;
- при нажатии «Запустить поток» несохранённые защищённые изменения не вызывают окно пароля: они откатываются, а поток запускается с сохранёнными параметрами;
- после добавления первой ссылки и сохранения настроек автоматический запуск сохраняется.

## 0.8.8 Beta

- при первой установке Monitor корректно работает без ссылки: после добавления первой действительной ссылки и сохранения настроек поток запускается автоматически;
- первый ручной запуск после первоначальной привязки также доступен без служебного пароля; все последующие ручные запуск, остановка и перезапуск остаются защищёнными;
- выбор записываемого монитора и другие разрешённые параметры автоматически пересоздают захват без запроса пароля администратора;
- при блокировке Windows настоящий экран Winlogon сохраняется на основном мониторе, а полностью чёрные области дополнительных мониторов заменяются фирменным экраном LinkVideo;
- добавлены режимы микрофона: постоянная передача, активация голосом и работа при удержании клавиши;
- добавлены настраиваемые глобальные горячие клавиши разговора и включения/выключения микрофона;
- микрофон захватывается постоянно отдельным локальным аудиоканалом, поэтому голосовая активация и горячие клавиши не перезапускают видеопоток;
- в интерфейсе появился живой индикатор активности и уровня микрофона;
- при отмене или неверном вводе пароля защищённые параметры автоматически возвращаются к сохранённым значениям;
- кнопка «Запустить поток» становится неактивной во время подключения и при работающем потоке; кнопки остановки и перезапуска также учитывают текущее состояние.

## 0.8.6 Beta

- установщик переведён на RichEdit для корректного отображения пользовательского соглашения;
- переходы между страницами установщика выполняются единым обновлением без дёргания и артефактов;
- выбор автозапуска убран из установщика: программа всегда настраивает запуск вместе с Windows;
- уточнён текст запроса пароля для защищённых настроек и управления трансляцией;
- добавлено предупреждение о закреплении IP-адреса компьютера при локальной RTSP-трансляции;
- код FFmpeg `4294967264` нормализуется в `-32` и отображается как закрытие соединения сервером.

## 0.8.5 Beta

- исправлено появление постоянного чёрного кадра после `Win+L`;
- последний подтверждённый кадр защищённого рабочего стола больше не перезаписывается временной чёрной поверхностью DXGI;
- добавлена дополнительная проверка чёрного кадра и в системном агенте, и в основном процессе;
- поток при блокировке и разблокировке продолжает работать без перезапуска;
- фирменная заглушка блокировки не возвращена.

## 0.8.4 Beta

- исправлена отрисовка установщика при переходах между страницами;
- программа устанавливается в `C:\Program Files\LinkVideo.Monitor`;
- исправлено создание ярлыков и ложное сообщение о незавершённой установке;
- на странице кодировщика показывается число вариантов для выбранного кодека;
- пароль требуется только для FPS, битрейта, автозапуска Windows, отключения индикатора и управления потоком;
- выбор экрана, курсор, защита паролей, звук, микрофон и перемещение индикатора сохраняются без пароля;
- поле пароля очищается сразу после подтверждения.

## 0.8.3 Beta

- Исправлено зависание мастера при запуске: Win32-интерфейс закреплён за одним OS-потоком, а соглашение больше не читается из крупного встроенного архива в момент открытия окна.
- Переработана первая страница: вместо технического списка добавлены брендированные карточки преимуществ и компактный блок системных требований.
- Исправлено положение текста «Запись экрана для бизнеса» в боковой панели.
- Добавлены лёгкие анимации появления карточек и акцентной линии при переходах между этапами.
- Добавлен полноценный многоразмерный значок для EXE, окна установщика, деинсталлятора и ярлыков.
- Фоновая установка и удаление остаются асинхронными, поэтому окно продолжает отвечать во время копирования файлов и работы службы.

## 0.8.2 Beta

- Новый полноценный пошаговый установщик в стиле LinkVideo.
- Добавлено обязательное принятие пользовательского соглашения.
- Добавлены видимые этапы и прогресс установки/обновления.
- Добавлены параметры ярлыка рабочего стола и автозапуска.
- Добавлен отдельный брендированный мастер удаления.
- При удалении можно сохранить или очистить настройки и журналы.
- Команда удаления из приложения перенаправляется на новый мастер.

## 0.8.1 Beta

- Пароль администратора больше не создаётся клиентом: в сборку встроен управляемый LinkVideo пароль, а старый локальный файл пароля удаляется при запуске.
- Клиент не может изменить пароль через интерфейс или локальный API.
- Расширен скрытый API дистанционного управления до версии 2: FPS, битрейт, кодек, кодировщик, разрешение, режим/монитор захвата, курсор, защита полей, системный звук, микрофон, индикатор записи, автозапуск и параметры сна.
- Добавлены дистанционные команды запуска, остановки и перезапуска потока, а также перезапуска приложения.
- Monitor передаёт серверу доступные кодировщики и допустимые значения, а недоступный аппаратный кодировщик отклоняет с причиной.
- Добавлена поддержка Bearer-ключа API, встраиваемого при производственной сборке и не показываемого клиенту.

## 0.8.0 Beta

- Проверка подключения теперь показывает конкретный доступный сервер.
- Для локальной RTSP-трансляции добавлена проверка занятости порта до сохранения и запуска.
- В интерфейсе протокол отображается как «RTSP» без технической подписи TCP.
- Добавлена сохраняемая история переподключений с датой, временем, кодом завершения и причиной каждого события; последняя причина остаётся в сводке.
- Доступны 15, 25 и 30 кадров/с; значение по умолчанию — 15.
- Добавлен H.265 для программного кодирования, Intel Quick Sync, NVIDIA NVENC и AMD AMF.
- Перед показом кодировщиков программа проверяет их реальную доступность через комплектный FFmpeg и скрывает неподдерживаемые варианты.
- Добавлен отдельный захват микрофона, выбор устройства ввода и смешивание микрофона с системным звуком в одну AAC-дорожку.
- Первый защищённый шаг создаёт пароль администратора; дальнейшее изменение настроек, положения индикатора, запуск, остановка и перезапуск потока требуют этот пароль.
- Кнопка сохранения неактивна, пока настройки не изменены.
- Вместо сохранения журнала через браузер добавлено копирование полного журнала в буфер обмена.
- Искусственная заглушка LinkVideo при Win+L/UAC убрана: используется настоящий защищённый кадр, а до его появления сохраняется последний реальный кадр без подмены.

## 0.7.15 Beta

- Устранён разрыв RTSP при Win+L, разблокировке и возврате из сна: кодировщик больше не перезапускается ради смены битрейта.
- Кадры защищённого рабочего стола хранятся отдельно от обычного захвата и больше не перезаписываются фоновым Desktop Duplication/GDI.
- Экран Winlogon сохраняется в потоке даже когда он статичен.
- Автоматическое переключение на 512 Кбит/с VBR убрано, поскольку оно требовало разрыва активного RTSP-сеанса.

## 0.7.14 Beta

- Исправлено обновление поверх установленной версии: установщик сначала останавливает службу и фоновые агенты, затем с повторными попытками заменяет занятый EXE.
- Последний настоящий кадр экрана Winlogon сохраняется при статичном экране блокировки и больше не заменяется плашкой через 1–2 секунды.
- При блокировке автоматически включается внутренний профиль 512 Кбит/с VBR; после разблокировки возвращаются обычные параметры.
- Выбор CBR/VBR и блок «Дополнительные настройки» удалены из интерфейса.

## 0.7.13 Beta

- Защищённый экран Windows теперь захватывается через DXGI Desktop Duplication из отдельного процесса `LOCAL_SYSTEM` в сеансе пользователя.
- Служба запускает защищённый агент с токеном процесса `winlogon.exe` нужного сеанса вместо простой подмены Session ID у токена службы.
- Защищённый агент запускается из установленной папки программы и использует комплектный `ffmpeg.exe` с фильтром `ddagrab`.
- Объект Desktop Duplication автоматически пересоздаётся после переключения рабочего стола, потери доступа или отсутствия новых кадров.
- Добавлены отдельные журналы `secure-capture.log` и расширенная диагностика запуска защищённого агента.
- При обычном `Win+L` добавлена короткая бесшовная передача кадра, чтобы заглушка LinkVideo не вспыхивала до появления первого кадра Winlogon.

## 0.7.12 Beta

- Индикатор записи автоматически переносится на выбранный монитор, если сохранённая позиция находится на другом экране.
- Положение индикатора сохраняется, если оно уже находится внутри захватываемого монитора.
- Защищённый агент явно привязывает поток к рабочему столу `Winlogon` перед началом захвата.
- Во время блокировки защищённый GDI-захват заново получает desktop DC для каждого кадра, чтобы не использовать поверхность, открытую до `Win+L`.
- Фирменная заглушка LinkVideo остаётся резервом только до появления настоящего кадра экрана блокировки.

## 0.7.11 Beta

- Служба Windows автоматически запускает фоновый агент Monitor в активном пользовательском сеансе, независимо от готовности автозагрузки Explorer.
- Индикатор «Ведётся запись экрана» получил автоматический перезапуск после входа в Windows и при неожиданном завершении.
- При блокировке сеанса защищённый агент принудительно принимает кадр рабочего стола Winlogon, поэтому фирменная заглушка используется только до появления настоящего экрана блокировки.
- Служба сохраняет совместимость с обновлением поверх предыдущих beta-версий и продолжает использовать прежнее имя службы.

## 0.7.10 Beta

- Перенесён выбор CBR/VBR в раскрывающийся блок «Дополнительные настройки».
- Нижняя часть настроек качества приведена к единому визуальному стилю.

## 0.7.9 Beta

- Программный H.264 теперь выбран по умолчанию.
- Режим «Автоматически» удалён из списка кодировщиков.
- Intel Quick Sync, NVIDIA NVENC и AMD AMF оставлены для ручного выбора.
- Интервал ключевых кадров возвращён к фиксированным 2 секундам для всех кодировщиков, чтобы не ухудшать работу облачного архива.

## 0.7.8 Beta

- Добавлен выбор режима битрейта CBR/VBR; по умолчанию используется CBR.
- Для Intel Quick Sync увеличен интервал ключевых кадров, чтобы уменьшить периодическую пульсацию всего экрана на низком битрейте.
- Убрана подсказка о мелком тексте и ограничении качества.

# LinkVideo Monitor 0.7.6 Beta

- Исправлена гонка поколений процесса: завершение старого FFmpeg больше не сбрасывает состояние уже запущенного нового потока после быстрого перезапуска.
- Защищён локальный веб-интерфейс: изменяющие состояние запросы требуют POST и фирменный заголовок, проверяется loopback-адрес и Host; устранён CSRF со сторонних сайтов.
- При занятом порту 8098 приложение проверяет подлинность уже запущенного LinkVideo Monitor и больше не передаёт приватную ссылку подключения постороннему локальному процессу.
- `SetThreadExecutionState` перенесён на постоянный закреплённый поток Windows, поэтому запрет сна и его отмена теперь выполняются на одном OS thread.
- Обновление heartbeat защищённого захвата стало атомарным: служба больше не видит ложное исчезновение запроса и не перезапускает агент UAC/Winlogon.
- Ужесточена проверка запросов к системной службе: проверяются session ID, PID, имя процесса, точное имя shared-memory, возраст и размер файла.
- Сырые GDI-кадры и PCM-звук теперь гарантированно записываются полностью даже при частичном `Write`.
- Запись кадра в FFmpeg больше не удерживает блокировку состояния захвата, поэтому событие блокировки Windows не задерживается зависшим pipe.
- Настройки сохраняются атомарно; повреждённый конфиг не перезаписывается значениями по умолчанию при автозапуске.
- Установщик и обновление MediaMTX заменяют файлы атомарно, не удаляя рабочую версию до успешной замены.
- Исправлен отложенный перезапуск после сна: остановленный пользователем поток больше не запускается обратно.
- Неподдерживаемая дистанционная команда больше не помечается выполненной; размер ответа API ограничен.
- Убран оставшийся `CAPTUREBLT` из резервного кода предпросмотра.

# LinkVideo Monitor 0.7.5 Beta

- При блокировке Windows сначала передаётся настоящий экран Winlogon, полученный службой защищённого захвата.
- Фирменный экран LinkVideo с логотипом используется только как безопасный резерв, пока защищённый кадр недоступен.
- Последний кадр разблокированного рабочего стола по-прежнему никогда не показывается после `Win+L`.
- После разблокировки обычная трансляция восстанавливается автоматически без перезапуска RTSP.

# LinkVideo Monitor 0.7.4 Beta

- Исправлена утечка последнего кадра после блокировки Windows: вместо оставленного рабочего стола показывается безопасный экран состояния.
- Добавлено определение блокировки текущего пользовательского сеанса через WTS.
- Во время блокировки всегда показывается нейтральный экран состояния, поэтому содержимое рабочего стола не остаётся видимым.
- При переходе на UAC или Winlogon до получения первого защищённого кадра больше не показывается старый рабочий стол.

# LinkVideo Monitor 0.7.3 Beta

- Исправлено перекрытие часов Windows индикатором «Ведётся запись экрана».
- Положение индикатора теперь рассчитывается по реальной рабочей области выбранного монитора через `SPI_GETWORKAREA`/`GetMonitorInfoW`, а не по жёсткому отступу 48 пикселей.
- Сохранённая позиция автоматически переносится внутрь рабочей области, если после смены масштаба, разрешения или расположения панели задач она попала на панель задач.

# LinkVideo Monitor 0.7.2 Beta

- Исправлено яростное мигание физического курсора при установке и других переходах, временно переключающих захват с Desktop Duplication на GDI.
- Из GDI-захвата убран флаг `CAPTUREBLT`: на части Windows и видеодрайверов он скрывал и заново показывал системный курсор при каждом кадре.
- Курсор в трансляции по-прежнему отрисовывается вручную через `GetCursorInfo`/`DrawIconEx`.

## 0.7.1 Beta

- Переработана защита конфиденциальных полей в браузерах.
- Сохраняется автоматическая защита паролей, PIN, CVV/CVC, номера карты и OTP/2FA.
- Анализируется только фактически сфокусированное поле ввода, а не текст всей страницы.
- Убраны частичные совпадения: «код» больше не срабатывает внутри слова «кодировщик».
- Длинный текст страниц и сообщений не используется как признак конфиденциального поля.
- Маски больше не хранятся как неподвижные экранные прямоугольники: подтверждённые поля отслеживаются через UI Automation, перемещаются вместе со страницей и удаляются при исчезновении элемента, смене вкладки или окна.
- Подавлены колебания координат UI Automation на 1–3 пикселя, вызывавшие дрожание вкладок и текста.

## 0.7.0 Beta

## Защищённый рабочий стол и UAC

- Добавлена системная служба `LinkVideoMonitorCapture`, работающая от `LocalSystem`.
- Служба запускает отдельный GDI-агент в активном сеансе на `winsta0\Winlogon`.
- Кадры защищённого рабочего стола передаются основному приложению через именованную shared-memory область текущего сеанса.
- FFmpeg, кодировщик и RTSP/RTMP-соединение остаются в основном процессе и не перезапускаются при переходе на UAC.
- При возврате на обычный рабочий стол захват автоматически переключается обратно.
- Не добавляется отдельная заглушка «Рабочий стол заблокирован»: передаётся фактический защищённый рабочий стол.

## Защита конфиденциальных данных

- Убрано скрытие всех `Edit` и `ComboBox` элементов.
- Всегда скрываются элементы с `UIA_IsPassword=true`.
- Для обычных полей применяется строгая проверка названия, AutomationId, класса, HelpText и ARIA-метаданных.
- Добавлены пароли, PIN, одноразовые коды, CVV/CVC, номера карт, банковские и паспортные реквизиты, API-ключи и токены.
- Адресная строка, поиск, email, комментарии и обычные текстовые поля браузера не скрываются.
- Добавлены тесты правил для Chrome, Edge и Firefox.

## Кодировщики

- Аппаратный кодировщик проверяется на фактическом разрешении и FPS трансляции.
- Выбранный автоматически кодировщик закрепляется и не меняется при сетевом переподключении.
- Убрана ложная блокировка GPU из-за общего сообщения `Error while opening encoder`.
- Переход на `libx264` происходит только после аппаратно-специфической ошибки.
- После подтверждённой ошибки аппаратный кодировщик получает период ожидания 30 минут; после повторной ошибки — 2 часа.
- Для NVENC, QSV и AMF используется входной формат NV12.

## Установка

- Установщик сохраняет прежний простой интерфейс.
- Для установки системной службы отдельно запрашиваются права администратора.
- Служебный EXE размещается в `%PROGRAMDATA%\LinkVideo.Monitor\Service`.
- Деинсталлятор удаляет службу, агент, session-файлы и данные предыдущих версий.

## 0.7.7 Beta

- Защита конфиденциальных полей выключена по умолчанию.
- Расширено распознавание CVV/CVC и сохранение маски во время перезагрузки страницы.
- Кодирование переведено с жёсткого CBR на ограниченный VBR; улучшены пресеты аппаратных кодировщиков и преобразование мелких деталей интерфейса.
- Добавлена компенсация стабильного отставания системного звука и синхронизация через `aresample`.
- Добавлена кнопка «Проверить обновления» и API манифеста обновлений; адрес встраивается при сборке без хранения токена приватного GitHub в приложении.
