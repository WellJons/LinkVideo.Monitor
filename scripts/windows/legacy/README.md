# Старые вспомогательные сценарии

Эти файлы сохранены из исходной структуры 0.7.1 для истории и диагностики.
Они предполагают, что EXE, документация и FFmpeg лежат рядом, поэтому для обычной сборки используйте сценарии уровнем выше:

- `build-app.cmd`
- `build-installer.cmd`
- `build-release.cmd`
